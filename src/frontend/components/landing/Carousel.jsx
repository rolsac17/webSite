import React from 'react';
import '../../assets/styles/components/landign/Carousel.scss';

const Carousel = ({ children }) => {
  return <div className='categoria__container'>{children}</div>;
};

export default Carousel;
